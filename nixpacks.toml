[phases.setup]
    nixPkgs = ['dotnet-sdk_8']

[phases.install]
    cmds = ['dotnet restore']

[phases.build]
    cmds = ['dotnet publish MusicProject.sln -c Release -o .']

[start]
    cmd = "dotnet MusicProjectAPI.dll"
