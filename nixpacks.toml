[phases.setup]
    nixPkgs = ['dotnet-sdk_8']

[phases.install]
    cmds = ['dotnet restore']

[phases.build]
    cmds = ['dotnet publish MusicProject.sln -c Release -o out']

[start]
    cmd = "dotnet out/MusicProjectAPI.dll --urls http://0.0.0.0:$PORT"
